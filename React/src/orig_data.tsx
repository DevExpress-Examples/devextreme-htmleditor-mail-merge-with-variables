export interface Name {
  FirstName: string;
  LastName: string;
}

const markup: string = `
<p>This is a demo to illustrate how to parse and convert variables. To get started, input several variables into the editor, type the variable's values in the TextBoxes below, and click the "Convert HtmlEditor variables" button.</p>
`;

export default markup;
