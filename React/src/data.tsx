export interface Variable {
  [key: string]: string;
}
const markup = '<p>This demo illustrates how to parse and convert variables. To get started, input several variables into the editor, type the variable\'s values in the TextBoxes below, and click the "Convert HtmlEditor variables" button.</p>';

export const DX_VARIABLE_CLASS = 'dx-variable';
export const DATA_VAR_VALUE_ATTR = 'data-var-value';

export default markup;
